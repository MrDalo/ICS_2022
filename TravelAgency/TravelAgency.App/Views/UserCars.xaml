<UserControl x:Class="TravelAgency.App.Views.UserCars"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TravelAgency.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource DefaultForegroundBrightBrush}">

        

        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ListView HorizontalContentAlignment="Right"  d:ItemsSource="{d:SampleData ItemCount=3}">
            <!--<ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>-->
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="150" Height="280"> <!--Height="250"-->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="150" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Image Height="120" Width="140" Grid.Row="0" Source="https://www.diethelmtravel.com/wp-content/uploads/2016/04/bill-gates-wealthiest-person-279x300.jpg" Stretch="UniformToFill" />
                        
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Grid.Row="0" Content="ŠPZ:" />
                            <Label Grid.Column="0" Grid.Row="1" Content="Výrobca:" />
                            <Label Grid.Column="0" Grid.Row="2" Content="Kapacita:" />
                            <Label Grid.Column="0" Grid.Row="3" Content="Typ:"/>
                            <Label Grid.Column="0" Grid.Row="4" Content="Registrácia:"/>

                            <Label Grid.Column="1" Grid.Row="0" Content="binding" />
                            <Label Grid.Column="1" Grid.Row="1"  Content="binding" /> 
                            <Label Grid.Column="1" Grid.Row="2" Content="binding" />
                            <Label Grid.Column="1" Grid.Row="3"  Content="binding" />
                            <Label Grid.Column="1" Grid.Row="4"  Content="binding" />
                        </Grid>
                       
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Label Grid.Column="1" Content="todo add car button"></Label>


    </Grid>
</UserControl>
